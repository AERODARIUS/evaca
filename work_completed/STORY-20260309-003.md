# STORY-20260309-003: Extract shared alert form state + market data client

## What was implemented
- Extracted eToro callable access into a typed reusable service: `web/src/lib/marketDataClient.ts`.
- Extracted alert form state machine and side effects into `web/src/hooks/useAlertEditor.ts`.
- Centralized reset/edit initialization logic in `buildInitialEditorState` so state setup lives in one place.
- Refactored `AlertForm` into a mostly presentational component consuming `useAlertEditor` state/actions.
- Added tests covering:
  - service success and invalid response handling (`marketDataClient.test.ts`)
  - hook helper initialization, validation-failure submit, and async save failure/success paths (`useAlertEditor.test.ts`).

## Files changed
- `app/web/src/lib/marketDataClient.ts`
- `app/web/src/hooks/useAlertEditor.ts`
- `app/web/src/components/AlertForm.tsx`
- `app/web/src/lib/marketDataClient.test.ts`
- `app/web/src/hooks/useAlertEditor.test.ts`
- `TODO.md`

## Tests executed and results
- `npm run web:test` (from `app/`) -> passed (24/24 tests)

## Notes / limitations
- `AlertForm` remains responsible for JSX rendering and accessibility wiring; business logic now resides in hook/service.
