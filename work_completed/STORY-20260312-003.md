# STORY-20260312-003: Consolidate duplicate CI workflow sources

## What was implemented
- Removed duplicated deprecated workflows from `app/.github/workflows`.
- Kept `/.github/workflows` as the canonical CI/deploy workflow location.
- Split chained CI command execution into named workflow steps for clearer failing gate identification.
- Documented canonical workflow path and named CI quality gates in `app/README.md`.

## Files changed
- `.github/workflows/firebase-hosting-pull-request.yml`
- `.github/workflows/firebase-hosting-merge.yml`
- `app/.github/workflows/firebase-hosting-pull-request.yml` (deleted)
- `app/.github/workflows/firebase-hosting-merge.yml` (deleted)
- `app/README.md`
- `TODO.md`

## Tests executed and results
- `cd app && npm run functions:lint` ✅ pass
- `cd app && npm run functions:test:coverage` ✅ pass
- `cd app && npm run functions:test:rules` ✅ pass

## Notes/limitations
- Coverage command still uses current project thresholds; this story focused on workflow consolidation and named CI steps.
