# STORY-20260312-001: Add TTL lifecycle controls for operational collections

## What was implemented
- Added `cleanupOperationalCollections` scheduled Cloud Function (every 60 minutes).
- Added bounded deletion utility to prune operational collections by timestamp.
- `_rateLimits` cleanup now removes expired documents (`expiresAt <= now`).
- `schedulerLeases` cleanup now removes stale leases (`leaseExpiresAt <= now - retention`).
- Added runbook documentation with deploy, verification, and growth monitoring steps.

## Files changed
- `app/functions/index.js`
- `app/functions/index.test.js`
- `app/docs/ttl-lifecycle-runbook.md`
- `app/README.md`
- `TODO.md`

## Tests executed and results
- `cd app && npm run functions:lint` ✅ pass
- `cd app && npm run functions:test` ✅ pass (45 tests)

## Notes/limitations
- Cleanup strategy uses scheduled deletes (equivalent to TTL lifecycle behavior) rather than managed Firestore TTL policy configuration.
