# STORY-20260312-004: Add Remote Config log-scope feature flag

## What was implemented
- Added shared frontend logging utility (`web/src/lib/logger.ts`) with scoped logs.
- Added runtime verbose-logging flag control via Firebase Remote Config key `logs_verbose_enabled`.
- Set safe default to disabled and fetch/activate behavior in `web/src/lib/firebase.ts`.
- Routed alert-editor workflow logs through the shared logger wrapper.
- Added unit tests for logger behavior with verbose flag enabled/disabled.

## Files changed
- `app/web/src/lib/logger.ts`
- `app/web/src/lib/logger.test.ts`
- `app/web/src/lib/firebase.ts`
- `app/web/src/hooks/useAlertEditor.ts`
- `app/README.md`
- `TODO.md`

## Tests executed and results
- `cd app && npm run web:test` ✅ pass (8 files, 27 tests)

## Notes/limitations
- Remote Config fetch is best-effort at startup; when unavailable, verbose logging defaults to disabled.
