# STORY-20260311-001: Align Firestore rules and indexes with due-alert scheduling

## What was implemented
- Updated Firestore rules for `alerts` to include and validate `nextCheckAt` as part of required and allowed fields.
- Added composite Firestore indexes for:
  - due-alert scheduler query: `isActive + nextCheckAt`
  - alert pagination query: `userId + createdAt + __name__`
- Added backend contract tests that assert the rules/index contracts include `nextCheckAt` and required composite index signatures.

## Files changed
- `app/firestore.rules`
- `app/firestore.indexes.json`
- `app/functions/index.test.js`
- `TODO.md`

## Tests executed and results
- `npm run functions:test` (from `app/`) ✅ passed (33/33)

## Notes/limitations
- Validation here is contract-level and static for indexes/rules presence; runtime emulator security regression tests are handled in `STORY-20260311-003`.
