# STORY-20260311-005: Upgrade runtime from Node.js 20 before EOL cutoff

## What was implemented
- Upgraded Functions runtime engine target from Node `20` to Node `22`.
- Upgraded Firebase Functions runtime configuration from `nodejs20` to `nodejs22`.
- Updated CI workflows to run with `node-version: 22`.
- Updated project docs to reflect Node.js 22 prerequisite.
- Added local runtime pin file `.nvmrc` in `app/` with `22`.
- Aligned lockfile workspace engine metadata to Node `22`.

## Files changed
- `.github/workflows/firebase-hosting-merge.yml`
- `.github/workflows/firebase-hosting-pull-request.yml`
- `app/.nvmrc`
- `app/README.md`
- `app/firebase.json`
- `app/functions/package.json`
- `app/package-lock.json`
- `TODO.md`

## Tests executed and results
- `npm run functions:test` (from `app/`) ✅ passed (40/40)
- `npm run functions:test:coverage` (from `app/`) ✅ passed

## Notes/limitations
- Validation in this environment executed on local Node `v20.17.0`; CI/runtime configs are now pinned to Node 22.
