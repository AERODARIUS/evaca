# STORY-20260311-003: Add Firestore rules security regression tests

## What was implemented
- Added a dedicated Firestore rules regression test suite in `app/functions/firestore.rules.test.js`.
- Added rule checks for:
  - owner-only reads for `alerts` and `notifications`
  - `alerts` update ownership and `createdAt` immutability constraints
  - blocked client writes for `notifications`
- Added test scripts:
  - `functions:test:rules` at app level
  - `test:rules` at functions workspace level
- Wired both GitHub workflows to run `npm run functions:test:rules` as part of CI gating.

## Files changed
- `.github/workflows/firebase-hosting-merge.yml`
- `.github/workflows/firebase-hosting-pull-request.yml`
- `app/functions/firestore.rules.test.js`
- `app/functions/package.json`
- `app/package.json`
- `TODO.md`

## Tests executed and results
- `npm run functions:test` (from `app/`) ✅ passed (40/40)
- `npm run functions:test:rules` (from `app/`) ✅ passed (3/3)

## Notes/limitations
- Network restrictions in this execution environment prevented installing `@firebase/rules-unit-testing` (`ENOTFOUND registry.npmjs.org`), so this run implemented contract-level regression tests against `firestore.rules` source and enforced them in CI.
