# STORY-20260310-004: Externalize Firebase runtime config by environment

## What was implemented
- Replaced hardcoded Firebase web config in `web/src/lib/firebase.ts` with typed environment variables.
- Added startup validation via `getRequiredEnv(...)` so app boot fails fast with actionable messages when env vars are missing.
- Expanded `web/.env.example` to include all required Firebase identifiers.
- Added strict env typings in `web/src/vite-env.d.ts`.
- Updated README with environment-specific setup guidance for local, preview/CI, and production.

## Files changed
- `app/web/src/lib/firebase.ts`
- `app/web/.env.example`
- `app/web/src/vite-env.d.ts`
- `app/README.md`
- `TODO.md`

## Tests executed and results
- `npm run web:test` (from `app/`) -> passed (19/19 tests)

## Notes / limitations
- Deploy pipelines must provide all required `VITE_FIREBASE_*` variables before build; otherwise startup fails by design.
